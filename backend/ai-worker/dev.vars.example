# Production uses Cloudflare Secrets Store, not .dev.vars.
# Binding: OPENAI_API_KEY -> account secret named SpectraPRO.
# For local Worker development, create a local Secrets Store secret with Wrangler
# (omit --remote) rather than placing the real production key in this file.
